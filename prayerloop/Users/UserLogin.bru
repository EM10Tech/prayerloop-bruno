meta {
  name: UserLogin
  type: http
  seq: 2
}

post {
  url: {{siteUrl}}/login
  body: json
  auth: none
}

body:json {
  {
    "username": "admin",
    "password": "admin"
  }
}

script:post-response {
  bru.setEnvVar("jwt",res.body.token);
}
